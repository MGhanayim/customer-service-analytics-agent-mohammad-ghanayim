# Pinned to the exact versions this project was developed and tested against,
# so the grader's `pip install -r requirements.txt` reproduces our environment.
# (Developed on Python 3.11.)
langgraph==1.1.9
langgraph-checkpoint-sqlite==3.1.0
langchain-core==1.3.2
langchain-nebius==0.1.3
datasets==4.8.5
pandas==3.0.2
pydantic==2.12.5
python-dotenv==1.2.2
fastmcp==3.3.1
streamlit==1.57.0
pytest==9.0.3
